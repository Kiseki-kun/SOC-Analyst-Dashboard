# Runtime dependencies for the SOC Analyst Dashboard backend.
# Upper bounds are pinned to the current major so an unattended `docker build`
# months from now cannot silently pull a breaking release.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
email-validator>=2.2,<3.0

SQLAlchemy>=2.0.30,<2.1
alembic>=1.13,<2.0
# psycopg 3 (binary build: no local libpq/compiler needed in the image).
psycopg[binary]>=3.2,<4.0

# JWT. PyJWT is actively maintained; python-jose is not.
PyJWT>=2.9,<3.0
# bcrypt is used directly rather than through passlib: passlib 1.7.4 predates
# bcrypt 4.x and emits a version-detection error against modern bcrypt.
bcrypt>=4.2,<6.0

structlog>=24.4,<26.0
httpx>=0.27,<1.0
